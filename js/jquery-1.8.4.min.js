function getInputValue(div_name,inputID) {
    var _div = document.getElementById(div_name);
    var inputValue;
    for(var i=0;i<_div.getElementsByTagName("input").length;i++){
        if(_div.getElementsByTagName("input")[i].id==inputID){
            inputValue=_div.getElementsByTagName("input")[i].value;
        }
    }
    return inputValue;
}
	/*
    var userName=document.getElementById("userName").value;
    var pwd=document.getElementById("pwd").value;
    if(userName=="admin" &&  pwd=="123456"){
		event.preventDefault();
		$('form').fadeOut(500);
		$('.wrapper').addClass('form-success');
		requestFullScreen();
		setTimeout(function(){location.href="index1.html";},2000);
		//auto_link_html("index1.html");
	}
	else{
		alert("不对不对,重新输入!");
	}
	*/
var btn = document.getElementById("login-button");
btn.onclick =function(){
    var userName = (getInputValue("userbox","userName"));
    var pwd= (getInputValue("pwdbox","pwd"));
    if(userName=="potato" &&  pwd=="991122"){
        setTimeout(function(){location.href="/html/index1.html";},1000);
        //auto_link_html("index1.html");
    }
    else{
        alert("不对不对,重新输入!");
    }
}

function requestFullScreen(element) {
	var element=document.documentElement;
 // 判断各种浏览器，找到正确的方法
 //alert(element.webkitRequestFullScreen);
 var requestMethod = element.requestFullScreen || //W3C
 element.webkitRequestFullScreen || //Chrome等
 element.mozRequestFullScreen || //FireFox
 element.msRequestFullScreen; //IE11
 if (requestMethod) {
  requestMethod.call(element);
 }
 else if (typeof window.ActiveXObject !== "undefined") {//for Internet Explorer
  var wscript = new ActiveXObject("WScript.Shell");
  if (wscript !== null) {
   wscript.SendKeys("{F11}");
  }
 }
}
function changeFrameHeight(){
    var ifm= document.getElementById("iframepage"); 
    ifm.height=document.documentElement.clientHeight;

}

window.onresize=function(){  
     changeFrameHeight();  

} 

// function auto_link_html(url){
// 	var ifm = document.getElementById("frame_content");
// 	ifm.setAttribute("src", url);
// }
